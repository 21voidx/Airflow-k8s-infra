apiVersion: v1
kind: Service
metadata:
  name: airflow-worker
  namespace: airflow
spec:
  clusterIP: None
  selector:
    app: airflow-worker
  ports:
    - name: worker-logs
      port: 8793
      targetPort: 8793