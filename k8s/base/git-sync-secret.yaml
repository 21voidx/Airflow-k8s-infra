# Template only.
# Gunakan PAT read-only atau fine-grained token jika repo airflow-dags private.
apiVersion: v1
kind: Secret
metadata:
  name: git-sync-secret
  namespace: airflow
type: Opaque
stringData:
  username: x-access-token
  password: CHANGE_ME_GITHUB_TOKEN
